#include "main.h"

/**
 *
 *
 *
 *
 *
 *
 *
 *
 */

int *array_range(int min, int max)
{
  unsigned int *p;
  unsigned int size, i;
  
  if (min > max)
    return (NULL);

  size = max - min;

  P = malloc(sizeof(int) * size);

  for (i = 0; i <= size; i++)
 p[i]=mi
}